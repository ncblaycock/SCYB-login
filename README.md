# Next.js Login App (TypeScript)

A minimal Next.js application with a login page that posts credentials to the server, written in TypeScript.

## Run locally

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start development server:
   ```bash
   npm run dev
   ```
3. Open http://localhost:3000/login
